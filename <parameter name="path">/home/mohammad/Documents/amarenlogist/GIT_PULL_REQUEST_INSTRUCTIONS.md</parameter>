<parameter name="content"># Git Setup and Pull Request Instructions

## Quick Setup Steps

Since we have command execution issues, please run these commands manually in your terminal:

### 1. Configure Git (if not already configured)
```bash
git config user.name "AmarenLogist Bot"
git config user.email "bot@amarenlogist.com"
```

### 2. Check repository status
```bash
git status
```

### 3. Add all changes
```bash
git add .
```

### 4. Commit with descriptive message
```bash
git commit -m "Fix: Resolve authentication error and port configuration

- Fixed TypeError: Invalid URL in OAuth authentication
- Added missing environment variables (VITE_OAUTH_PORTAL_URL, VITE_APP_ID)
- Configured proper backend/frontend URLs  
- Application now running successfully on port 3006
- Created .env.dev file with proper development environment setup"
```

### 5. Create new branch
```bash
git checkout -b blackboxai/fix-authentication-issues
```

### 6. Set up remote origin
```bash
git remote remove origin 2>/dev/null || true
git remote add origin https://github.com/MohammadAmarena/AmarenLogist.git
```

### 7. Push to GitHub
```bash
git push -u origin blackboxai/fix-authentication-issues
```

### 8. Create Pull Request
1. Go to https://github.com/MohammadAmarena/AmarenLogist
2. Click "Compare & pull request" for the `blackboxai/fix-authentication-issues` branch
3. Add a title: "Fix: Resolve Authentication Error and Port Configuration"
4. Add description: Include the changes made and testing results
5. Click "Create pull request"

## Changes Made
- ✅ Fixed TypeError: Invalid URL in OAuth authentication
- ✅ Added missing environment variables (.env.dev file)
- ✅ Configured proper backend/frontend URLs
- ✅ Application now running successfully on port 3006
- ✅ No runtime errors, full functionality restored</parameter>
